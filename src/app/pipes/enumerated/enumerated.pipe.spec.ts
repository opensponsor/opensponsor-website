import { EnumeratedPipe } from './enumerated.pipe';

describe('EnumeratedPipe', () => {
  it('create an instance', () => {
    const pipe = new EnumeratedPipe();
    expect(pipe).toBeTruthy();
  });
});
