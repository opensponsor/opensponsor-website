import { BlobDirective } from './blob.directive';

describe('BlobDirective', () => {
  it('should create an instance', () => {
    const directive = new BlobDirective();
    expect(directive).toBeTruthy();
  });
});
